const output_file = "service_entry.txt";

// Example usage: writing to the file
fs.writeFileSync(output_file, "Hello, world!");

// Example usage: passing the file path to a function
function readFile(filePath) {
    // Logic to read the file contents
}

readFile(output_file);
